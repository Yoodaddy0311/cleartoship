# PRD: Sprint 3 Wrap & Next-Session Handoff (Autopilot Night Session)

**Session ID**: `ap-20260517-014225`
**Mode**: `night` (PushNotification 차단, 큐만 누적)
**Max**: `4h` / `2,000,000 tokens`
**Checkpoint**: `30m` WIP commit
**Worktree**: 미사용 (한글 cwd 환경 — ASCII tmpdir fallback 회피)
**Goal Contract**: 없음 (legacy 7-phase 흐름 — outcome 자동화 아닌 process 자동화)
**Drafted**: 2026-05-17 01:42:25 (사용자 취침 직전)

---

## 1. 배경 (Background)

Sprint 3 Polish & Performance(2026-05-17) 진행 중 — 옵션 3 ("새로운 작업") 선택으로 S3-10 잔여 백로그 착수 중.

**현재 시점 상태** (PRD 작성 직전 스냅샷):

| 항목 | 값 |
|------|-----|
| Branch | `main` (remote 미설정 — push BLOCKED) |
| Local commits ahead | 13건 (Sprint 0+1 시드 1 + Sprint 2 7 + Sprint 3 wave1 4 + Sprint 3 docs 1 + autopilot 미적용) |
| Last commit SHA | `4aa3b77 docs(sprint3): CHANGELOG + session handoff` |
| /team session | `team-sprint3-s310` 활성 — e2e-runner (S3-10A) in_progress, frontend-developer (S3-10B) completed |
| Dev server (3100) | `.next` 캐시 corruption HTTP 500 (사용자 환경에서 재기동 필요) |
| 검증 baseline | web 380/380, functions 32/32, E2E 15/15, tsc clean |

**S3-10B 완료 산출물** (frontend-developer 보고):
- `apps/web/lib/i18n/en.ts` (131개 키, Messages 타입 ko 기반)
- `apps/web/lib/i18n/en.test.ts` (parity + 빈 값 가드 — 5 tests)
- `lib/i18n/` 8 tests PASS, tsc clean

**S3-10A 진행 중** (e2e-runner working tree):
- `apps/web/e2e/marketing-smoke.spec.ts` (modified — networkidle 제거)
- `apps/web/e2e/pages/MarketingHomePage.ts` (modified)
- `apps/web/e2e/axe.spec.ts` (modified)
- `apps/web/e2e/golden-path.spec.ts` (modified)
- `apps/web/e2e/prd-upload.spec.ts` (미수정 — 다음)
- `apps/web/e2e/url-validation.spec.ts` (미수정 — 다음)
- networkidle 호출 grep: 0 hits (완료)

---

## 2. 목표 (Objectives)

| ID | 목표 | 검증 방법 |
|----|------|----------|
| O1 | S3-10A 완료 흡수 — e2e-runner 보고 수신 + commit 생성 | git log에 `feat(e2e):...` 또는 `chore(e2e):...` 단일 commit 존재 |
| O2 | S3-10B 완료 흡수 — frontend-developer 결과 commit 생성 | git log에 `feat(i18n): en.ts scaffolding` commit 존재 |
| O3 | /team Phase 4 cross-check 통과 — code-reviewer 2명(sonnet) 동시 검증 | 2건 모두 APPROVE 또는 REQUEST_CHANGES 처리 완료 |
| O4 | /team Phase 4.5 inspection 통과 — code-reviewer 1명 최종 검수 5체크 | INSPECTION REPORT 5/5 PASS |
| O5 | CHANGELOG.md Sprint 3 섹션에 S3-10 항목 추가 | grep "S3-10" CHANGELOG.md → ≥3 hits |
| O6 | reports/AUTOPILOT/ap-20260518-handoff.md 생성 — 다음 세션 진입 가이드 | 8섹션 템플릿 준수 (1.Current 2.Commits 3.Verified 4.Important 5.Outstanding 6.TeamState 7.Files 8.Checklist) |
| O7 | 모든 검증 clean — test/tsc/e2e | web≥380 PASS, functions≥32 PASS, tsc 0 error, marketing-smoke 9/9 (--repeat-each=3) |
| O8 | reports/AUTOPILOT/ap-20260517-014225.md 자율세션 보고서 생성 | PRD §13.5 템플릿 준수 |

---

## 3. 비목표 (Non-Goals)

- **Push to remote**: `git remote -v` 공란. 사용자가 URL 제공 전까지 push 시도 금지. Phase 4 VERIFY에서도 push 명령 차단.
- **Audit-start form 재마운트**: Sprint 4 스코프 (현재 `apps/web/components/audit-start/url-input-form.tsx`는 코드 존재하나 라우트에 미마운트. S3-10A에서 outdated specs를 skip 처리하는 것이 본 세션의 한계).
- **Dev server 재기동**: 사용자 환경 책임 (`.next` cache corruption은 사용자가 `rm -rf .next` 후 재기동).
- **새로운 feature 개발**: Sprint 3 폴리시/검증 마무리만 수행. 신규 컴포넌트/API/스키마 추가 금지.
- **선존 tsc 에러 수정**: `functions/src/lib/enqueue-audit-task.test.ts:40` — Sprint 2 `5aec9a4` 유입분. 본 자율세션 무관, 별도 ticket 필요.
- **Goal Contract (v4.6.0)**: outcome 자동화 미적용. 본 세션은 process 자동화 — Phase 5 후 iteration loop 없음.
- **외부 데이터 송신**: DATA POLICY §5.5 엄격 준수. WebFetch/WebSearch/외부 MCP 호출 0건.

---

## 4. 시나리오 (Scenarios)

### 4.1 정상 시나리오 (Happy Path)

```
T+0:00  PRD 작성 완료 (Phase 0)
T+0:00  Phase 1 PLAN 진입 — planner agent 소환
T+0:05  e2e-runner 완료 보고 수신 (S3-10A)
T+0:10  Phase 2 EXECUTE 진입 — cross-check 2명 + inspector 1명 동시 spawn
T+0:25  cross-check 모두 APPROVE 보고
T+0:35  inspection APPROVE 보고
T+0:40  S3-10A commit 생성 (e2e 파일 6건)
T+0:45  S3-10B commit 생성 (i18n 파일 2건)
T+0:50  CHANGELOG.md Edit (Sprint 3 섹션에 S3-10 추가)
T+0:55  CHANGELOG commit
T+1:00  Phase 3 CROSS_CHECK — spec-reviewer (sonnet) 소환 (원형 검증)
T+1:15  Phase 4 VERIFY — pnpm -r test + tsc + marketing-smoke E2E
T+1:40  Phase 5 IMPROVE — refactor-cleaner + performance-engineer 병렬
T+2:10  ap-20260518-handoff.md 작성 (doc-updater sonnet)
T+2:20  handoff commit 생성
T+2:25  Phase 6 REPORT — autopilot 세션 보고서 작성
T+2:30  세션 종료 + 사용자 큐 정리
```

### 4.2 e2e-runner timeout 시나리오

```
T+0:00  PRD 완료
T+0:30  e2e-runner 30분 무응답 → engine.shouldPause() = true (장시간 무진행)
T+0:31  PAUSED + 큐: "e2e-runner timeout — S3-10A 수동 확인 필요"
T+0:31  fallback: 현재까지 변경된 4개 파일만으로 부분 commit (chore 라벨)
T+0:32  남은 2개 파일 (prd-upload, url-validation)는 다음 세션 큐에 적재
T+0:35  Phase 3 이후 정상 흐름
```

### 4.3 검증 실패 시나리오

```
Phase 4 VERIFY:
  attempt 1: pnpm -F web test → 2 FAIL (regression)
  → build-error-resolver agent 자동 소환 (Sprint 4-ready)
  attempt 2: → 1 FAIL
  → build-error-resolver retry
  attempt 3: → 1 FAIL
  → PAUSED + 마지막 정상 SHA (`4aa3b77`) 복원 제안 + 큐 적재
```

### 4.4 위험 트리거 시나리오 (모든 모드 즉시 종료)

- `git push --force`, `git branch -D main`, `rm -rf /` 패턴 감지 시 즉시 abort
- 외부 API key / serviceAccount*.json staging 시도 시 abort
- 외부 host로 fetch/POST 시도 시 DATA POLICY abort

---

## 5. 설계 (Phase 0-6 상세 설계)

### 5.0 Phase 0 — INTAKE ✅ (현재 단계)

**책임**: 본 PRD 작성. 모든 Phase의 success criteria, 산출물, 리스크를 정의.

**입력**:
- 사용자 task description: "plan phase6까지의 내용과 작업을 정말 디테일하게 설계해줄래?"
- 현재 컨텍스트: /team session 진행 중, Sprint 3 마무리 단계

**출력**:
- `docs/PRD/sprint3-wrap-ap-20260517-014225.md` (이 파일)

**도구**: Write tool (Claude Code 메인)

**완료 조건**: 본 파일 디스크 저장 완료. `:resume ap-20260517-014225` 가능 상태.

---

### 5.1 Phase 1 — PLAN

**책임**: PRD를 작업 단위로 분해, 위험 식별, 병렬 팀 구성 제안.

**소환 agent**: `artibot:planner` (model: opus 4.7, effort: xhigh, task-budget: 128000)

**프롬프트 템플릿**:
```
[Autopilot Phase 1] PRD: docs/PRD/sprint3-wrap-ap-20260517-014225.md

분해 + 위험 식별 + 병렬 팀 구성 제안.

기존 /team(team-sprint3-s310) 결과를 활용해야 함:
- e2e-runner (S3-10A) — 진행 중 (working tree에 4개 modified 파일 + 2개 미수정)
- frontend-developer (S3-10B) — completed (2개 untracked 파일)

Wave 분해 권장:
- Wave A: e2e-runner 완료 흡수 + 부분 commit OR 전체 commit
- Wave B: frontend-developer 결과 commit (en.ts/en.test.ts)
- Wave C: cross-check 2명(sonnet) + inspector 1명(sonnet) 병렬
- Wave D: CHANGELOG.md edit + commit
- Wave E: 검증 (test/tsc/e2e)
- Wave F: handoff 보고서 작성

각 Wave에 owner agent, 입출력 파일, 검증 명령, 의존성 명시.
```

**산출물**:
- `reports/AUTOPILOT/ap-20260517-014225-plan.md` — Wave 분해 표 + 의존성 그래프

**완료 조건**: Wave A~F 모두 owner/입력/출력/검증 정의됨. 의존성 그래프에 순환 없음.

**예상 소요**: 5분

---

### 5.2 Phase 2 — PARALLEL EXECUTE

**책임**: 실제 코드 변경 + commit 생성. 30분마다 WIP checkpoint commit.

**팀 구성** (`team-sprint3-s310` 재활용 + 필요 시 추가 소환):

| Wave | Owner | 입력 | 출력 | 의존성 | 모델 |
|------|-------|------|------|--------|------|
| A | e2e-runner (기존) | 진행 중 작업 완료 | 6개 e2e 파일 modified | - | opus |
| B | frontend-developer (기존) | 보고 완료 | en.ts, en.test.ts (이미 disk에 존재) | - | opus |
| C1 | code-reviewer #1 (신규 sonnet) | Wave A 산출물 | APPROVE/CHANGES report | A | sonnet |
| C2 | code-reviewer #2 (신규 sonnet) | Wave B 산출물 | APPROVE/CHANGES report | B | sonnet |
| C3 | code-reviewer inspector (신규 sonnet) | Wave A+B 통합 | INSPECTION REPORT 5체크 | A, B | sonnet |
| D1 | 메인 (리더) | Wave A APPROVE | commit `feat(e2e): wait strategy + outdated spec skip` | C1 | - |
| D2 | 메인 (리더) | Wave B APPROVE | commit `feat(i18n): en.ts scaffolding + parity tests` | C2 | - |
| E | 메인 (리더) | Wave D 완료 | CHANGELOG.md edit + commit `docs(sprint3): S3-10 changelog` | D1, D2 | - |
| F | doc-updater (신규 sonnet) | E 완료 | `reports/AUTOPILOT/ap-20260518-handoff.md` | E | sonnet |

**체크포인트 정책**:
- 매 30분: `git commit -m "wip(autopilot): phase2 checkpoint ap-20260517-014225"` (스테이징된 변경분만, 없으면 스킵)
- 체크포인트 SHA를 `engine.recordCheckpoint(sessionId, sha)`로 기록

**팀원 spawn 시 디렉티브**:
```
[artibot:effort level=xhigh command=autopilot][artibot:task-budget max_tokens=128000]
```

**커밋 메시지 컨벤션**:

```
feat(e2e): wait strategy + outdated spec skip (S3-10A)

- Playwright wait strategy: networkidle → domcontentloaded
  · marketing-smoke.spec.ts + pages/MarketingHomePage.ts
- Outdated E2E spec skip (until audit-start form re-mounted):
  · axe.spec.ts: /audits 케이스만 test.skip
  · golden-path/prd-upload/url-validation: 전체 describe.skip
- 검증: networkidle grep 0 hits, marketing-smoke --repeat-each=3 → 9/9 PASS

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

```
feat(i18n): en.ts scaffolding + parity tests (S3-10B)

- apps/web/lib/i18n/en.ts: English locale map, 131 keys, ko-derived type
- apps/web/lib/i18n/en.test.ts: ko parity + empty-value guard + non-ASCII key guard
- index.ts default export 미변경 (여전히 ko 반환)
- 검증: lib/i18n/ 8 tests PASS, tsc clean

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

```
docs(sprint3): CHANGELOG S3-10 entries

- Added: en.ts/en.test.ts (S3-10B)
- Changed: Playwright wait strategy + 4 outdated spec skip (S3-10A)
- Verified: web/functions/E2E/tsc 모두 PASS

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

```
docs(autopilot): session handoff for ap-20260518

- 8 sections (Current State / Commits / Verified / Important / Outstanding / Team / Files / Checklist)
- 16 local commits ahead (still push BLOCKED)
- Sprint 3 백로그 비움 (S3-10 완료) — Sprint 4 진입 준비
- 다음 세션 진입 시 검토할 audit-start form 재마운트 결정

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

**예상 소요**: 60-90분

**위험**:
- e2e-runner 미완료 — fallback: 30분 timeout 후 부분 commit (Wave A 부분 적용)
- cross-check가 REQUEST_CHANGES — 해당 팀원 재소환 후 수정 (최대 2회 retry)

---

### 5.3 Phase 3 — CROSS_CHECK

**책임**: 팀원 간 원형 검증 (A→B→C→A) + 추가 spec-reviewer 소환.

**소환 agent**:
- `artibot:spec-reviewer` (sonnet) — 본 세션 변경분이 PRD §2 목표와 1:1 일치하는지 검증

**프롬프트**:
```
[Autopilot Phase 3 / Spec-reviewer]

PRD: docs/PRD/sprint3-wrap-ap-20260517-014225.md
변경된 SHA 범위: 4aa3b77..HEAD

검증 항목:
1. PRD §2 O1~O8 각 목표가 commit에 1:1 매핑되는지
2. PRD §3 비목표(push, audit-start 재마운트, 외부 데이터 송신)가 위반되지 않았는지
3. scope creep 검출 — 본 세션 무관 파일 변경 있는지 (특히 `functions/`, `packages/ui/`, `workers/`)
4. 누락 — PRD에 명시된 산출물 중 빠진 항목 있는지

REPORT 형식: 목표별 ✅/❌ + 상세 사유.
```

**완료 조건**: spec-reviewer APPROVE 또는 명확한 REQUEST_CHANGES.

**예상 소요**: 10분

---

### 5.4 Phase 4 — VERIFY

**책임**: 자동 검증 명령 실행. 실패 시 build-error-resolver 자동 소환, 3회 재시도.

**검증 시퀀스** (병렬 실행):

| 명령 | Expected | 실패 시 |
|------|----------|---------|
| `pnpm -F web test` | ≥380 PASS, 0 FAIL | build-error-resolver 소환 |
| `pnpm -F functions test` | ≥32 PASS, 0 FAIL | build-error-resolver 소환 |
| `pnpm -F web exec tsc --noEmit` | 0 error | build-error-resolver 소환 |
| `pnpm -F web exec playwright test marketing-smoke.spec.ts --repeat-each=3` | 9/9 PASS, 0 flake | e2e-runner 재소환 |
| `pnpm -F @cleartoship/shared-types exec tsc --noEmit` | 0 error | typescript-pro 소환 |

**선존 에러 면제**: `functions/src/lib/enqueue-audit-task.test.ts:40` — Sprint 2 유입분, 본 세션 무관 (재현 시 검증 통과로 인정).

**재시도 정책**:
- 1회: 자동 build-error-resolver 소환 → re-run
- 2회: 동일 에러면 typescript-pro 추가 소환
- 3회: PAUSED + 마지막 정상 SHA(`4aa3b77`) 복원 제안 + 큐 적재

**예상 소요**: 20분 (재시도 없을 시)

---

### 5.5 Phase 5 — IMPROVE

**책임**: 품질 개선 제안 — 자율 실행하되 PRD §3 비목표 위반 시 보고서에만 기록.

**병렬 소환**:

1. `artibot:refactor-cleaner` (opus)
   - 프롬프트: "본 세션 변경분(4aa3b77..HEAD)에 한정하여 dead code / unused imports / duplicate logic 점검. 제거 안전한 항목만 별도 commit."
   - 출력: `chore(cleanup): dead code removal` commit OR 보고서에 발견사항만 기록

2. `artibot:performance-engineer` (opus)
   - 프롬프트: "Sprint 3 변경분의 Core Web Vitals 영향 측정. graph-canvas 지연 prefetch, marketing 페이지 LCP/INP 베이스라인 기록. 회귀 없으면 보고서에만 기록, 회귀 시 patch 제안."
   - 출력: 보고서 §7 성능 베이스라인

**완료 조건**: 두 agent 모두 보고 완료. 추가 commit은 사용자 검토 큐에 적재.

**예상 소요**: 25분

---

### 5.6 Phase 6 — REPORT

**책임**: 자율 세션 최종 보고서 작성.

**소환 agent**: `artibot:doc-updater` (sonnet)

**프롬프트**:
```
[Autopilot Phase 6 / Final Report]

세션: ap-20260517-014225
PRD: docs/PRD/sprint3-wrap-ap-20260517-014225.md
대상 파일: reports/AUTOPILOT/ap-20260517-014225.md

템플릿 (PRD §13.5):
1. 요약 — 1단락, 세션 목표 + 달성도
2. PRD 링크
3. Phase 0~6 표 — 상태 / 소요 / 변경 파일 / 검증
4. 커밋 SHA 리스트 — 본 세션 신규 commit 전부
5. Cross-check 결과 — spec-reviewer/code-reviewer/inspector 모두
6. 검증 결과 — test/tsc/e2e 출력 요약
7. 개선 사항 — refactor-cleaner + performance-engineer 보고
8. 미래 작업 (Sprint 4 후보) — audit-start 재마운트, push 설정, 선존 tsc 에러 수정
9. 큐 — 자율 실행 중 누적된 사용자 결정 요청
10. Next Action — 사용자 깨어났을 때 첫 실행 가이드
```

**산출물**:
- `reports/AUTOPILOT/ap-20260517-014225.md`
- 마지막 commit: `docs(autopilot): final report for ap-20260517-014225`

**완료 조건**: 보고서 디스크 저장 + 사용자 큐 검토 완료.

**예상 소요**: 15분

---

## 6. 산출물 (Deliverables Checklist)

### 6.1 코드 변경 (commits)

- [ ] `feat(e2e): wait strategy + outdated spec skip (S3-10A)` — 6 e2e 파일
- [ ] `feat(i18n): en.ts scaffolding + parity tests (S3-10B)` — 2 i18n 파일
- [ ] `docs(sprint3): CHANGELOG S3-10 entries` — CHANGELOG.md
- [ ] `docs(autopilot): session handoff for ap-20260518` — ap-20260518-handoff.md
- [ ] `docs(autopilot): final report for ap-20260517-014225` — ap-20260517-014225.md
- [ ] (조건부) `chore(cleanup): dead code removal` — Phase 5에서 안전 항목 발견 시
- [ ] (조건부) `perf: ...` — Phase 5에서 회귀 fix 필요 시

**예상 총 신규 commit**: 5~7건. 누적 local commits: 13 → 18~20.

### 6.2 문서

- [x] `docs/PRD/sprint3-wrap-ap-20260517-014225.md` (이 파일)
- [ ] `reports/AUTOPILOT/ap-20260517-014225-plan.md` (Phase 1 산출물)
- [ ] `reports/AUTOPILOT/ap-20260517-014225.md` (Phase 6 보고서)
- [ ] `reports/AUTOPILOT/ap-20260518-handoff.md` (Phase 2 Wave F 산출물)

### 6.3 검증 결과 기록

- [ ] web test PASS 수치 (실제 380 → ?)
- [ ] functions test PASS 수치 (실제 32 → ?)
- [ ] E2E 통과율 (--repeat-each=3 기준)
- [ ] tsc 결과 (clean 여부)
- [ ] LCP/INP/CLS 베이스라인 (선택 — Phase 5)

---

## 7. 실행 계획 / 타임라인 (Timeline)

```
T+0:00  PRD 작성 (이 단계) ─────────────────── ✅
T+0:05  Phase 1 planner 소환 ────────────────── 5분
T+0:10  e2e-runner 보고 수신 + Wave A 흡수 ──── 의존
T+0:15  Phase 2 cross-check 2명 + inspector 1명 spawn (병렬)
T+0:30  cross-check + inspection 결과 수신
T+0:35  Wave D1 commit (S3-10A)
T+0:40  Wave D2 commit (S3-10B)
T+0:45  Wave E: CHANGELOG edit + commit
T+0:50  WIP checkpoint (30분 경과)
T+1:00  Phase 3 spec-reviewer 소환 (병렬로 Wave F doc-updater 진행)
T+1:15  spec-reviewer APPROVE 수신
T+1:20  Wave F handoff 보고서 작성
T+1:30  handoff commit
T+1:35  Phase 4 VERIFY 시퀀스 (병렬 5개 명령)
T+2:00  VERIFY 결과 수신 (재시도 없을 시)
T+2:00  Phase 5 refactor-cleaner + performance-engineer 병렬 소환
T+2:25  Phase 5 결과 수신
T+2:30  Phase 6 doc-updater 소환 (자율세션 보고서)
T+2:45  보고서 commit
T+2:50  세션 종료 + 사용자 큐 정리
T+3:00  버퍼 30분 (재시도/예외)
T+4:00  Hard limit — 시간 초과 시 freeze + Phase 6 강제 진입
```

---

## 8. 위험 (Risks) 및 완화책

| 위험 | 확률 | 영향 | 완화 |
|------|------|------|------|
| e2e-runner timeout (>30분 무진행) | 中 | 中 | fallback: 현재 4개 파일만 부분 commit + 미수정 2개는 다음 세션 큐 |
| cross-check REQUEST_CHANGES (1회) | 中 | 低 | 해당 팀원 재소환, 수정 후 재검수 (최대 2회 retry) |
| cross-check REQUEST_CHANGES (3회) | 低 | 高 | PAUSED + 마지막 정상 SHA 복원 + 사용자 큐 적재 |
| VERIFY 5회 실패 | 低 | 高 | safety §5.5 — PAUSED + 디버그 로그 보존 |
| 빌드 실패 3회 | 中 | 高 | build-error-resolver + 마지막 정상 SHA(`4aa3b77`) 복원 제안 |
| context > 85% | 中 | 中 | 자동 strategic-compact + checkpoint commit |
| 시간 초과 (>4h) | 低 | 中 | freeze + Phase 6 강제 진입 + 진행상태 보고 |
| destructive command (force-push, branch -D 등) | 極低 | 極高 | safety §5.5 — 모든 모드 즉시 pause |
| 외부 데이터 송신 시도 | 極低 | 極高 | DATA POLICY 위반 → 즉시 abort + 보고 |
| 한글 cwd path issue | 低 | 中 | worktree 미사용 (이미 결정), Write tool 절대 경로 사용 |
| Sprint 3 외 scope 침범 | 中 | 中 | spec-reviewer가 Phase 3에서 검출, 발견 시 revert |
| en.ts 영문 번역 품질 부족 | 中 | 低 | Phase 3 spec-reviewer 검토, 보고서 §8 미래작업으로 적재 |
| Push BLOCKED (remote 없음) | 高 | 低 | 모든 commit은 local only. push 시도 금지 (safety hook) |

---

## 9. 수락 기준 (Acceptance Criteria)

세션 성공 = 아래 모든 항목 PASS:

- [ ] **AC1**: Phase 0~6 모두 DONE 또는 PAUSED(사유 명시)로 종료
- [ ] **AC2**: O1~O8 중 ≥7개 목표 달성 (O7 검증은 1개 부분 실패 허용 — 단 선존 에러만)
- [ ] **AC3**: 모든 commit이 conventional commit 형식 (`feat/fix/docs/chore/perf(scope): ...`)
- [ ] **AC4**: 모든 commit이 `Co-Authored-By: Claude Opus 4.7` 트레일러 포함
- [ ] **AC5**: 본 세션 신규 commit ≥5건
- [ ] **AC6**: `reports/AUTOPILOT/ap-20260517-014225.md` 디스크 존재 + 10섹션 모두 채워짐
- [ ] **AC7**: `reports/AUTOPILOT/ap-20260518-handoff.md` 디스크 존재 + 8섹션 모두 채워짐
- [ ] **AC8**: 본 PRD §3 비목표 위반 0건 (push, audit-start 재마운트, 외부 송신, 선존 에러 수정 등)
- [ ] **AC9**: `git status --short` clean 또는 untracked만 존재 (modified/staged 0건)
- [ ] **AC10**: 사용자 큐(queuedQuestions)는 자율 결정 불가 항목만 누적 (단순 정보성 X)

---

## 10. Goal Contract (v4.6.0) — 미사용

본 세션은 **process 자동화** (legacy 7-phase 흐름). Goal Contract 슬롯 미적용 — Phase 5 후 evaluator 미실행, Phase 2 재진입 없음. iteration loop 없음.

**미사용 사유**:
- Sprint 3 마무리는 outcome 명확하지 않음 (CHANGELOG 추가는 binary success criteria 없음)
- iteration이 도움 안 됨 — 1회 실행으로 충분
- validationCommand=`npm run ci`는 본 repo에 없음 (`pnpm -r test` 분산)

---

## 11. DATA POLICY 준수 선언

- 외부 DB 호출: ❌ 금지
- 외부 플러그인 호출: ❌ 금지 (`mcp__pencil__*`, `mcp__claude_ai_*` 등 사용 금지)
- 외부 HTTP fetch: ❌ 금지 (WebFetch/WebSearch agent 직접 호출 금지)
- 외부 API key 노출: ❌ 금지 (.env / serviceAccount*.json 스테이징 금지)
- 외부 호스트로 데이터 송신: ❌ 금지
- 로컬 파일 시스템 한정: ✅ Write/Edit/Read만 사용
- git remote: ❌ 미설정 상태 유지 (push 시도 금지)

---

## 12. 사용자 큐 (Initial Queue)

세션 시작 시 누적된 사용자 결정 요청 (Phase 6 보고서 §9에 통합):

1. **Q1**: `apps/web/app/audits/` 라우트에 `url-input-form` 재마운트 결정 필요 (Sprint 4 스코프 확정 시점)
2. **Q2**: git remote URL 제공 필요 — `git remote add origin <url> && git push -u origin main`
3. **Q3**: `functions/src/lib/enqueue-audit-task.test.ts:40` 선존 tsc 에러 수정 우선순위 (현재 Sprint 2 유입분으로 격리됨)
4. **Q4**: 영문 로케일 실제 활성화 시점 — `en.ts`는 스캐폴드 상태, 라우트 동적 스위치 미구현
5. **Q5**: Dev server (port 3100) `.next` 캐시 corruption — 다음 세션 시작 전 `rm -rf .next` 수행 여부

---

## 13. Phase Templates (참고)

### 13.5 Final Report Template (Phase 6 산출물 구조)

```markdown
# Autopilot Session Report — <sessionId>

## 1. 요약
{1-2 단락, 세션 목표 + 달성도 % + 핵심 변경}

## 2. PRD
- 링크: docs/PRD/<feature>-<sessionId>.md
- 모드: night
- 시작: <ts>
- 종료: <ts>
- 소요: <duration>

## 3. Phase 진행
| Phase | 상태 | 소요 | 변경 파일 | 검증 |
|-------|------|-----|----------|------|
| 0 INTAKE | DONE | ... | ... | - |
| 1 PLAN | DONE | ... | ... | - |
| 2 EXECUTE | DONE | ... | ... | ... |
| 3 CROSS_CHECK | DONE | ... | - | spec-reviewer APPROVE |
| 4 VERIFY | DONE | ... | - | ... |
| 5 IMPROVE | DONE | ... | ... | ... |
| 6 REPORT | DONE | ... | reports/AUTOPILOT/<sessionId>.md | - |

## 4. Commits (신규)
| SHA | Message |
|-----|---------|
| ... | ... |

## 5. Cross-check
| 검토자 | 대상 | 결과 | 피드백 |
|--------|------|------|--------|
| ... | ... | APPROVE/CHANGES | ... |

## 6. 검증
{test/tsc/e2e 출력 요약}

## 7. 개선 (Phase 5)
- refactor-cleaner: {발견사항}
- performance-engineer: {베이스라인 + 회귀 여부}

## 8. 미래 작업 (Sprint 4 후보)
1. ...

## 9. 큐 (Queued Questions)
| # | 사유 | 위험도 |
|---|------|--------|
| ... | ... | ... |

## 10. Next Action
{사용자 깨어났을 때 첫 실행 가이드}
```

### 13.6 Handoff Template (Wave F 산출물 구조)

```markdown
# Session Handoff — <date>

## 1. Current State
- Branch / Working tree / Dev server / Port 상태

## 2. Commits Landed This Session
| SHA | Message |

## 3. Verified
| 대상 | 결과 |

## 4. Important / Caveats
{선존 에러, 한계, 알려진 이슈}

## 5. Outstanding / What's Pending
{잔여 백로그}

## 6. Team State
{idle agent 리스트}

## 7. Files NOT in commit chain
{untracked 또는 staged 안 된 파일}

## 8. Re-entry checklist
{다음 세션 첫 실행 명령 시퀀스}
```

---

## 14. 다음 세션 (ap-20260518) 진입 가이드

본 세션 종료 후 사용자 깨어났을 때 첫 실행:

```bash
# 1. 상태 확인
cd "C:/Users/nowhe/OneDrive/바탕 화면/AI/ClearToShip/cleartoship"
git log --oneline -10
git status --short

# 2. 자율 세션 보고서 확인
cat reports/AUTOPILOT/ap-20260517-014225.md

# 3. 다음 세션 핸드오프 확인
cat reports/AUTOPILOT/ap-20260518-handoff.md

# 4. 누적된 큐 확인 (자율 세션이 결정 못 한 항목)
/autopilot:status ap-20260517-014225

# 5. Dev server 재기동 (캐시 corruption 해소)
cd apps/web && rm -rf .next && pnpm exec next dev -p 3100 &

# 6. Sprint 4 진입 또는 큐 처리
```

---

## 15. 사용자 승인 요청

본 PRD는 `/autopilot:plan` dry-run 모드 결과물. 실제 자율 실행은 사용자가 깨어난 후:

```bash
/autopilot:resume ap-20260517-014225
```

또는 본 PRD를 검토 후 수정 사항이 있다면 직접 edit 후 resume.

**자율 실행 불가 시 (사용자 부재 + auto mode 비활성화)**: 본 PRD가 다음 세션 시작 가이드 역할만 수행 — 코드 변경 0건, 문서 1건만 추가.

---

**Drafted by**: Claude Opus 4.7 (Claude Code main agent)
**Drafted at**: 2026-05-17 01:42:25 KST (사용자 취침 직전)
**Next action**: `/autopilot:resume ap-20260517-014225` (사용자 깨어난 후)
