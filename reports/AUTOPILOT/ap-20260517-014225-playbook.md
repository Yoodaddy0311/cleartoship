# Autopilot Playbook — ap-20260517-014225

Wave-by-Wave 실행 가이드. PRD `docs/PRD/sprint3-wrap-ap-20260517-014225.md` §5 (Phase 설계) 의 운영 매뉴얼.

세션 진입 시 본 문서 + PRD 두 파일만 참조하면 Phase 0~6 자율 실행 가능.

---

## Wave 의존성 그래프

```
┌──────────────────────────────────────────────────────────┐
│ Wave A: e2e-runner 작업 완료 흡수 (기존 팀원)            │
│ Wave B: frontend-developer 작업 흡수 (이미 disk 존재)    │
└──────────┬─────────────────────────┬─────────────────────┘
           │                         │
           ▼                         ▼
┌──────────────────────┐  ┌──────────────────────┐
│ Wave C1: reviewer #1 │  │ Wave C2: reviewer #2 │
│   (Wave A 검증)      │  │   (Wave B 검증)      │
└──────────┬───────────┘  └──────────┬───────────┘
           │                         │
           └──────────┬──────────────┘
                      ▼
           ┌──────────────────────┐
           │ Wave C3: inspector   │
           │   (A+B 통합 5체크)   │
           └──────────┬───────────┘
                      │
           ┌──────────┴──────────┐
           ▼                     ▼
    ┌────────────┐         ┌────────────┐
    │ Wave D1    │         │ Wave D2    │
    │ commit A   │         │ commit B   │
    └─────┬──────┘         └─────┬──────┘
          │                      │
          └──────────┬───────────┘
                     ▼
           ┌──────────────────────┐
           │ Wave E: CHANGELOG    │
           │   edit + commit      │
           └──────────┬───────────┘
                     │
        ┌────────────┼────────────┐
        ▼            ▼            ▼
┌────────────┐ ┌──────────┐ ┌────────────┐
│Wave F:     │ │Wave G1:  │ │Wave G2:    │
│handoff doc │ │verify    │ │spec-review │
│(sonnet)    │ │(병렬)    │ │(sonnet)    │
└─────┬──────┘ └────┬─────┘ └─────┬──────┘
      │             │             │
      └─────────────┼─────────────┘
                    ▼
         ┌──────────────────────┐
         │ Wave H: improve      │
         │ refactor + perf 병렬 │
         └──────────┬───────────┘
                    ▼
         ┌──────────────────────┐
         │ Wave I: Phase 6 보고 │
         │ (doc-updater sonnet) │
         └──────────────────────┘
```

---

## Wave 상세 표

### Wave A — e2e-runner 완료 흡수

| 항목 | 값 |
|------|-----|
| Owner | `e2e-runner` (team-sprint3-s310, 기존) |
| 입력 | Task #1 (S3-10A) — 진행 중 |
| 출력 (예상) | 6개 e2e 파일 modified |
| 검증 | `grep -rn networkidle apps/web/e2e/` = 0 hits, `pnpm -F web exec playwright test marketing-smoke.spec.ts --repeat-each=3` = 9/9 PASS |
| 의존성 | (없음) |
| 시간 cap | 30분 (timeout 시 부분 commit fallback) |
| Fallback | 미수정 2개 파일(prd-upload, url-validation)은 다음 세션 큐로 |

**완료 조건**: e2e-runner가 SendMessage 통해 보고 + TaskUpdate completed.

**timeout 대응**:
```bash
# T+0:30에 미응답 시
cd "C:/Users/nowhe/OneDrive/바탕 화면/AI/ClearToShip/cleartoship"
git add apps/web/e2e/marketing-smoke.spec.ts apps/web/e2e/pages/MarketingHomePage.ts apps/web/e2e/axe.spec.ts apps/web/e2e/golden-path.spec.ts
git commit -m "feat(e2e): partial — wait strategy + 2 outdated skips (S3-10A partial)"
# 큐 적재: "S3-10A 잔여 — prd-upload + url-validation skip 처리 필요"
```

---

### Wave B — frontend-developer 결과 흡수

| 항목 | 값 |
|------|-----|
| Owner | `frontend-developer` (team-sprint3-s310, 기존) |
| 입력 | Task #2 (S3-10B) — completed |
| 출력 | `apps/web/lib/i18n/en.ts`, `apps/web/lib/i18n/en.test.ts` (이미 disk untracked 존재) |
| 검증 | `pnpm -F web test lib/i18n/` = 8 tests PASS, `pnpm -F web exec tsc --noEmit` clean |
| 의존성 | (없음) |
| 시간 cap | 즉시 (이미 완료) |
| Fallback | 불필요 |

**완료 조건**: 이미 충족.

**리더 액션**:
```bash
git status --short
# 확인: ?? apps/web/lib/i18n/en.ts
#       ?? apps/web/lib/i18n/en.test.ts
```

---

### Wave C1 — Cross-check #1 (Wave A 검증)

| 항목 | 값 |
|------|-----|
| Agent | `code-reviewer` (모델: sonnet — 검증은 sonnet으로 충분) |
| 입력 | Wave A 변경 파일 6개 |
| 출력 | APPROVE 또는 REQUEST_CHANGES + 상세 사유 |
| 의존성 | Wave A 완료 |

**Spawn 프롬프트**:
```
[artibot:effort level=high command=autopilot][artibot:task-budget max_tokens=40000]
[Cross-check Mode]

Autopilot 세션 ap-20260517-014225, Wave A (S3-10A) 검증.

검토 대상:
- apps/web/e2e/marketing-smoke.spec.ts
- apps/web/e2e/pages/MarketingHomePage.ts
- apps/web/e2e/axe.spec.ts
- apps/web/e2e/golden-path.spec.ts
- apps/web/e2e/prd-upload.spec.ts
- apps/web/e2e/url-validation.spec.ts

요구사항:
1. networkidle → domcontentloaded 전환이 모든 위치에 적용되었는지
2. outdated 4개 spec이 test.skip / describe.skip로 처리되었는지 (삭제 아님)
3. axe.spec.ts에서 마케팅 홈(/) 케이스는 유지, /audits 케이스만 skip되었는지
4. 각 skip된 spec 헤더에 "TODO Sprint 4" 주석이 있는지
5. tsc clean인지

스코프 외 변경 검출: 위 6개 파일 외 어떤 파일이라도 변경되었으면 REQUEST_CHANGES.

판정: APPROVE 또는 REQUEST_CHANGES + 상세.
```

**APPROVE 시**: Wave D1 진입.
**REQUEST_CHANGES 시**: e2e-runner 재소환 + 1회 retry. 2회 실패 시 PAUSED.

---

### Wave C2 — Cross-check #2 (Wave B 검증)

| 항목 | 값 |
|------|-----|
| Agent | `code-reviewer` (모델: sonnet, 별도 인스턴스) |
| 입력 | Wave B 변경 파일 2개 |
| 출력 | APPROVE 또는 REQUEST_CHANGES |
| 의존성 | Wave B 완료 |

**Spawn 프롬프트**:
```
[artibot:effort level=high command=autopilot][artibot:task-budget max_tokens=40000]
[Cross-check Mode]

Autopilot 세션 ap-20260517-014225, Wave B (S3-10B) 검증.

검토 대상:
- apps/web/lib/i18n/en.ts
- apps/web/lib/i18n/en.test.ts

요구사항:
1. en.ts의 키 집합이 ko.ts와 1:1 동일한지 (en.test.ts parity 테스트가 검증)
2. en.test.ts가 다음 가드 포함하는지:
   - en/ko 키 parity
   - 빈 문자열 값 없음
   - 비-ASCII 키 없음
3. Messages 타입이 `{ [K in keyof Ko]: string }` 패턴으로 ko-derived인지
4. index.ts default export 미변경 (여전히 ko 반환)
5. 영문 번역이 placeholder("TODO" 등) 아닌 실제 번역인지 — random 5개 키 스폿체크
6. tsc clean, lib/i18n/ tests PASS

스코프 외 변경 검출: ko.ts / index.ts / ko.test.ts 등 무관 파일 변경 시 REQUEST_CHANGES.

판정: APPROVE 또는 REQUEST_CHANGES + 상세.
```

---

### Wave C3 — Inspector (5체크 통합 검수)

| 항목 | 값 |
|------|-----|
| Agent | `code-reviewer` (모델: sonnet, inspector 인스턴스) |
| 입력 | Wave A + Wave B 통합 산출물 |
| 출력 | INSPECTION REPORT 5체크 |
| 의존성 | Wave A + B 완료 (C1/C2 결과 활용) |

**Spawn 프롬프트**:
```
[artibot:effort level=high command=autopilot][artibot:task-budget max_tokens=40000]
[Inspection Mode]

Autopilot 세션 ap-20260517-014225, 최종 검수.

원본 요청: Sprint 3 잔여 백로그 (S3-10) 처리.
- S3-10A: Playwright wait strategy + outdated spec 정리
- S3-10B: en.ts 멀티-로케일 스캐폴딩

각 팀원 작업물 검수:
1. e2e-runner — 6개 e2e 파일 변경
2. frontend-developer — 2개 i18n 파일 신규

검수 5체크 (PRD §5.5 inspection checklist):
1. 요청 일치 — 원본 요청 vs 실제 변경 1:1 대조
2. 범위 준수 — 요청 범위 밖 파일 변경 없는지
3. 무결성 — 기존 기능 파손 없는지 (테스트 통과)
4. 품질 — 프로젝트 패턴/컨벤션 준수
5. 부작용 — 불필요한 추가/변경 없는지

REPORT 형식:
| 대상 | 요청일치 | 범위준수 | 무결성 | 품질 | 부작용 | 판정 |
|------|:-------:|:-------:|:-----:|:----:|:-----:|------|
| e2e-runner (S3-10A) | ✅/❌ | ✅/❌ | ✅/❌ | ✅/❌ | ✅/❌ | APPROVE/CHANGES |
| frontend-developer (S3-10B) | ✅/❌ | ✅/❌ | ✅/❌ | ✅/❌ | ✅/❌ | APPROVE/CHANGES |

전체 판정: APPROVE / REQUEST_CHANGES / REJECT
```

**APPROVE 시**: Wave D 진입.
**REQUEST_CHANGES 시**: 해당 팀원 재소환 (1회 retry).
**REJECT 시**: PAUSED + 사용자 큐.

---

### Wave D1 — Commit S3-10A

| 항목 | 값 |
|------|-----|
| Owner | 메인 리더 (Claude Code) |
| 입력 | Wave C3 APPROVE |
| 출력 | git commit |
| 의존성 | C3 APPROVE |

**실행 시퀀스**:
```bash
cd "C:/Users/nowhe/OneDrive/바탕 화면/AI/ClearToShip/cleartoship"
git add apps/web/e2e/marketing-smoke.spec.ts
git add apps/web/e2e/pages/MarketingHomePage.ts
git add apps/web/e2e/axe.spec.ts
git add apps/web/e2e/golden-path.spec.ts
git add apps/web/e2e/prd-upload.spec.ts
git add apps/web/e2e/url-validation.spec.ts

git commit -m "$(cat <<'EOF'
feat(e2e): wait strategy + outdated spec skip (S3-10A)

- Playwright wait strategy: networkidle → domcontentloaded
  · marketing-smoke.spec.ts + pages/MarketingHomePage.ts
- Outdated E2E spec skip (until audit-start form re-mounted):
  · axe.spec.ts: /audits 케이스만 test.skip
  · golden-path/prd-upload/url-validation: 전체 describe.skip
- 검증: networkidle grep 0 hits, marketing-smoke --repeat-each=3 → 9/9 PASS

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

### Wave D2 — Commit S3-10B

| 항목 | 값 |
|------|-----|
| Owner | 메인 리더 |
| 입력 | Wave C3 APPROVE |
| 출력 | git commit |
| 의존성 | C3 APPROVE |

```bash
git add apps/web/lib/i18n/en.ts apps/web/lib/i18n/en.test.ts

git commit -m "$(cat <<'EOF'
feat(i18n): en.ts scaffolding + parity tests (S3-10B)

- apps/web/lib/i18n/en.ts: English locale map, 131 keys, ko-derived type
  · Messages = { [K in keyof Ko]: string } — 누락/오타 시 TS 컴파일 에러
- apps/web/lib/i18n/en.test.ts: ko parity + empty-value guard + non-ASCII key guard
- index.ts default export 미변경 (여전히 ko 반환)
- 검증: lib/i18n/ 8 tests PASS, tsc clean

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

### Wave E — CHANGELOG 갱신

| 항목 | 값 |
|------|-----|
| Owner | 메인 리더 (Edit tool) |
| 입력 | Wave D1, D2 SHA |
| 출력 | CHANGELOG.md modified + commit |
| 의존성 | D1, D2 완료 |

**Edit 위치**: `CHANGELOG.md` Sprint 3 섹션 (line 8~37)

**추가할 항목**:
```markdown
### Added
- ...(기존)
- `apps/web/lib/i18n/en.ts` — English locale map 131 keys, `Messages = { [K in keyof Ko]: string }` ko-derived 타입. index.ts default export 미변경 (스캐폴드 only). (S3-10B)
- `apps/web/lib/i18n/en.test.ts` — en/ko 키 parity + 빈 값 가드 + 비-ASCII 키 가드 (5 tests). (S3-10B)

### Changed
- ...(기존)
- `apps/web/e2e/marketing-smoke.spec.ts` + `pages/MarketingHomePage.ts` — Playwright wait strategy `networkidle` → `domcontentloaded`. (S3-10A)
- `apps/web/e2e/axe.spec.ts` — `/audits` 케이스만 test.skip + TODO Sprint 4 주석. 마케팅 홈(/) a11y는 유지. (S3-10A)
- `apps/web/e2e/golden-path.spec.ts`, `prd-upload.spec.ts`, `url-validation.spec.ts` — 전체 describe.skip + TODO Sprint 4 주석 (audit-start form 라우트 재마운트 시 재활성화 예정). (S3-10A)

### Verified
- ...(기존)
- networkidle 호출 0건 (grep)
- `pnpm -F web test lib/i18n/` → 8/8 PASS (en + ko)
- `pnpm -F web exec playwright test marketing-smoke.spec.ts --repeat-each=3` → 9/9 PASS, 0 flake
```

**Commit**:
```bash
git add CHANGELOG.md
git commit -m "$(cat <<'EOF'
docs(sprint3): CHANGELOG S3-10 entries

- Added: en.ts/en.test.ts (S3-10B)
- Changed: Playwright wait strategy + 4 outdated spec skip (S3-10A)
- Verified: 모든 검증 PASS

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

### Wave F — Handoff 보고서 작성 (다음 세션 진입 가이드)

| 항목 | 값 |
|------|-----|
| Agent | `doc-updater` (sonnet) |
| 입력 | Wave D + E SHA |
| 출력 | `reports/AUTOPILOT/ap-20260518-handoff.md` |
| 의존성 | E 완료 |

**Spawn 프롬프트**:
```
[artibot:effort level=medium command=autopilot][artibot:task-budget max_tokens=40000]

Autopilot ap-20260517-014225 Wave F.

작성 대상: reports/AUTOPILOT/ap-20260518-handoff.md
참조 템플릿: PRD §13.6 (Handoff Template)

내용:
1. Current State — branch, working tree, dev server, ports
2. Commits Landed This Session — Wave D1, D2, E + 자율세션 commits
3. Verified — 모든 검증 결과 표
4. Important / Caveats — push BLOCKED, dev server cache, 선존 tsc 에러
5. Outstanding / What's Pending — Sprint 4 백로그, audit-start 재마운트, en.ts 실제 활성화
6. Team State — team-sprint3-s310 멤버 상태
7. Files NOT in commit chain — untracked .artifacts/, 기타
8. Re-entry checklist — 다음 세션 첫 실행 명령

스타일: 기존 ap-20260517-handoff.md 형식 그대로.

완료 후 git에 add+commit 하지 말 것 — 리더가 일괄 commit.
```

**리더 commit**:
```bash
git add reports/AUTOPILOT/ap-20260518-handoff.md

git commit -m "$(cat <<'EOF'
docs(autopilot): session handoff for ap-20260518

- 8 sections (Current / Commits / Verified / Important / Outstanding / Team / Files / Checklist)
- 다음 세션 진입 가이드 (Sprint 3 완료 상태)
- Push 여전히 BLOCKED, 다음 세션도 remote URL 대기

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

### Wave G1 — Phase 4 VERIFY 시퀀스

| 항목 | 값 |
|------|-----|
| Owner | 메인 리더 (Bash 병렬) |
| 입력 | Wave E 완료 |
| 출력 | 검증 결과 표 |
| 의존성 | E 완료 |

**병렬 명령** (5개 동시 실행):
```bash
# Bash 1
cd "C:/Users/nowhe/OneDrive/바탕 화면/AI/ClearToShip/cleartoship" && pnpm -F web test 2>&1 | tail -20

# Bash 2
cd "C:/Users/nowhe/OneDrive/바탕 화면/AI/ClearToShip/cleartoship" && pnpm -F functions test 2>&1 | tail -20

# Bash 3
cd "C:/Users/nowhe/OneDrive/바탕 화면/AI/ClearToShip/cleartoship/apps/web" && pnpm exec tsc --noEmit 2>&1 | tail -20

# Bash 4
cd "C:/Users/nowhe/OneDrive/바탕 화면/AI/ClearToShip/cleartoship/packages/shared-types" && pnpm exec tsc --noEmit 2>&1 | tail -10

# Bash 5
cd "C:/Users/nowhe/OneDrive/바탕 화면/AI/ClearToShip/cleartoship/apps/web" && pnpm exec playwright test marketing-smoke.spec.ts --repeat-each=3 2>&1 | tail -20
```

**Expected**:
| 명령 | Expected |
|------|----------|
| web test | ≥380 PASS, 0 FAIL |
| functions test | ≥32 PASS, 0 FAIL |
| web tsc | clean (선존 enqueue-audit-task.test.ts:40 무관) |
| shared-types tsc | clean |
| marketing-smoke E2E | 9/9 PASS, 0 flake |

**실패 시**: PRD §5.4 재시도 정책 (build-error-resolver 자동 소환, 3회 cap).

---

### Wave G2 — Spec-reviewer (Phase 3)

| 항목 | 값 |
|------|-----|
| Agent | `spec-reviewer` (sonnet) |
| 입력 | 4aa3b77..HEAD 전체 변경 |
| 출력 | PRD §2 목표 1:1 검증 보고 |
| 의존성 | Wave D, E 완료 (G1과 병렬 가능) |

**Spawn 프롬프트**:
```
[artibot:effort level=medium command=autopilot][artibot:task-budget max_tokens=40000]
[Spec-reviewer Mode]

Autopilot ap-20260517-014225 Phase 3.

PRD: docs/PRD/sprint3-wrap-ap-20260517-014225.md
변경 범위: 4aa3b77..HEAD

검증:
1. PRD §2 O1~O8 각 목표 → 실제 commit 1:1 매핑 확인
2. PRD §3 비목표 위반 검출 (push 시도, audit-start 재마운트, 외부 송신, 선존 에러 수정 등)
3. Scope creep — Sprint 3 무관 파일 변경 (workers/, functions/<무관>, packages/ui/ 등)
4. 산출물 누락 — PRD §6에 명시된 항목 중 빠진 것

REPORT: 목표별 ✅/❌ + 사유 + 권고.
```

---

### Wave H — Phase 5 IMPROVE

| 항목 | 값 |
|------|-----|
| Agent | `refactor-cleaner` (opus) + `performance-engineer` (opus) |
| 입력 | Wave G 통과 |
| 출력 | 보고서 §7 데이터 |
| 의존성 | G1 + G2 모두 통과 |

**Spawn 1 — refactor-cleaner**:
```
[artibot:effort level=high command=autopilot][artibot:task-budget max_tokens=128000]

Autopilot ap-20260517-014225 Wave H1 / Phase 5.

본 세션 변경분(4aa3b77..HEAD)에 한정.

목표:
1. dead code / unused imports / duplicate logic 점검
2. 안전 제거 가능 항목만 별도 commit OR 보고서 §7에만 기록
3. 변경 범위 외 파일 수정 금지

도구: knip, depcheck, ts-prune (가능 시).

REPORT:
- 발견된 dead code 리스트 (파일:라인)
- 안전 제거 권고 (PR 분리 권고)
- 본 세션 별도 commit 생성 여부
```

**Spawn 2 — performance-engineer**:
```
[artibot:effort level=high command=autopilot][artibot:task-budget max_tokens=128000]

Autopilot ap-20260517-014225 Wave H2 / Phase 5.

Sprint 3 변경분의 Core Web Vitals 영향 측정.

대상:
- graph-canvas 지연 prefetch (S3-6, commit 70b2849)
- marketing 페이지 LCP/INP/CLS 베이스라인

방법:
- dev server (port 3100) 활성 시: playwright + web-vitals 측정
- dev server 비활성 시: 정적 분석 (bundle size, code split 효과)

REPORT:
- LCP/INP/CLS 베이스라인 수치 (가능한 경우)
- 회귀 감지 시 patch 제안
- Sprint 4 후보 개선안

주의: 외부 API 호출 금지 (Lighthouse CI 등 외부 서비스 ❌).
```

**결과 통합**: 두 agent 보고를 Phase 6 보고서 §7에 통합.

---

### Wave I — Phase 6 Final Report

| 항목 | 값 |
|------|-----|
| Agent | `doc-updater` (sonnet) |
| 입력 | 모든 Wave 결과 |
| 출력 | `reports/AUTOPILOT/ap-20260517-014225.md` |
| 의존성 | H 완료 |

**Spawn 프롬프트**:
```
[artibot:effort level=medium command=autopilot][artibot:task-budget max_tokens=40000]

Autopilot ap-20260517-014225 Phase 6 / Final Report.

작성 대상: reports/AUTOPILOT/ap-20260517-014225.md
템플릿: PRD §13.5

입력 데이터:
- PRD §2 O1~O8 달성도
- Wave A~I 각 결과 (Wave 표 데이터 통합)
- Commits SHA 리스트 (Wave D1, D2, E, F 등)
- Cross-check 결과 (C1, C2, C3 + G2)
- VERIFY 결과 (G1)
- IMPROVE 결과 (H1 refactor, H2 perf)
- 큐된 질문 (PRD §12 + 자율 실행 중 누적분)
- Sprint 4 후보 (미래 작업)

10 섹션 모두 채우기:
1. 요약
2. PRD 링크
3. Phase 0~6 진행 표
4. Commits 표
5. Cross-check 표
6. 검증 결과
7. 개선 사항
8. 미래 작업
9. 큐
10. Next Action

완료 후 git add+commit 하지 말 것 — 리더가 일괄.
```

**리더 commit**:
```bash
git add reports/AUTOPILOT/ap-20260517-014225.md docs/PRD/sprint3-wrap-ap-20260517-014225.md reports/AUTOPILOT/ap-20260517-014225-playbook.md

git commit -m "$(cat <<'EOF'
docs(autopilot): PRD + playbook + final report for ap-20260517-014225

- docs/PRD/sprint3-wrap-ap-20260517-014225.md: Phase 0~6 상세 설계
- reports/AUTOPILOT/ap-20260517-014225-playbook.md: Wave 운영 매뉴얼
- reports/AUTOPILOT/ap-20260517-014225.md: 자율 세션 최종 보고

세션 종료, persistent team idle, 다음 세션 진입 가이드 = ap-20260518-handoff.md

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## 체크포인트 정책

매 30분 경계에서:
```bash
git diff --cached --quiet || git commit -m "wip(autopilot): phase2 checkpoint ap-20260517-014225"
# (스테이징된 변경 없으면 스킵)
```

**기록**:
- WIP commit SHA를 `engine.recordCheckpoint(sessionId, sha)`로 기록 (가능 시)
- 미지원 환경: 본 playbook 하단 §체크포인트 로그에 수동 append

---

## 체크포인트 로그 (자율 실행 중 갱신)

| Time | SHA | Note |
|------|-----|------|
| (placeholder) | (TBD) | session start |
| ... | ... | ... |

---

## 큐 / Queued Questions (자율 실행 중 추가)

PRD §12 초기 큐 +

| # | Time | 사유 | 위험도 |
|---|------|------|--------|
| (Q6+) | ... | ... | ... |

---

## 안전 가드 빠른 참조

| 위험 | 액션 | 자동/수동 |
|------|------|-----------|
| `git push --force` 패턴 감지 | 즉시 abort | 자동 |
| `git branch -D main` 등 | 즉시 abort | 자동 |
| `rm -rf /` 패턴 | 즉시 abort | 자동 |
| serviceAccount*.json staging | 즉시 abort | 자동 |
| 외부 host POST/fetch | 즉시 abort + DATA POLICY 보고 | 자동 |
| test 5회 연속 실패 | PAUSED + 디버그 로그 보존 | 자동 |
| 빌드 3회 재시도 실패 | PAUSED + 마지막 정상 SHA(`4aa3b77`) 복원 제안 | 자동 |
| context > 85% | 자동 strategic-compact + checkpoint | 자동 |
| 토큰 예산 초과 | freeze + Phase 6 강제 진입 | 자동 |
| --max 시간 초과 (4h) | freeze + Phase 6 강제 진입 | 자동 |
| Cross-check REJECT | PAUSED + 사용자 큐 | 자동 |
| (unknown unknown) | 모든 모드 pause + 큐 기록 | 자동 |

---

**Drafted by**: Claude Opus 4.7
**Drafted at**: 2026-05-17 01:42:25
**Triggers actual execution via**: `/autopilot:resume ap-20260517-014225` (사용자 깨어난 후)
